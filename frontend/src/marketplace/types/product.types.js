// Plain JS "type documentation" shapes (no TypeScript in this
// project) — kept as JSDoc-style comments so future TS migration
// has a direct mapping target.

/**
 * @typedef {Object} ProductShape
 * @property {number} id
 * @property {string} name
 * @property {string} slug
 * @property {string} sku
 * @property {number} price
 * @property {string} currency
 * @property {string} status - one of ProductStatus enum values
 * @property {string[]} images
 */

module.exports = {};