// Placeholder — real barcode generation/validation (EAN-13, UPC,
// etc.) is out of scope until a phase explicitly requires it.
const generateBarcodePlaceholder = () => {
  throw new Error('Barcode generation is not implemented yet.');
};

module.exports = { generateBarcodePlaceholder };